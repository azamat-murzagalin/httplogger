name: http_ui_logger
description: Convenient way to store and present `http` client responses.
version: 0.0.2
homepage: https://github.com/azamat-murzagalin/http_ui_logger

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ">=0.12.0 <1.0.0"
  shared_preferences: ">=0.5.0 <1.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
